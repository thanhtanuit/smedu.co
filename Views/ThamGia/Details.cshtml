@model IEnumerable<Bidc_Union_Management.Models.DoanVien>
@using Bidc_Union_Management.Constant

<div class="sb2-2">
    <div class="sb2-2-2">
        <ul>
            <li><a href="/"><i class="fa fa-home" aria-hidden="true"></i>Home</a>
            </li>
            <li class="active-bre"><a href="#">Tham gia</a>
            </li>
        </ul>
    </div>
    <div class="sb2-2-3">
        <div class="row">
            <div class="col-md-12">
                <div class="box-inn-sp">
                    <div class="inn-title">
                        <h4>Danh sách đoàn viên tham gia hoạt động abc</h4>
                    </div>
                    <div class="tab-inn">
                        <div class="table-responsive table-desi">
                            @functions
                                        {
                                public string GenerateLinks(dynamic item)
                                {
                                    string result = string.Empty;
                                    MembershipUser user = Membership.GetUser();
                                    string[] roles = Roles.GetRolesForUser(User.Identity.Name);
                                    result += Html.ActionLink("Chi tiết ", "Details", "DoanVien",
                                                               new
                                                               {
                                                                   id = item.Id
                                                               },
                                                               new { @title = "Chi Tiết" }
                                                               ).ToString();
                                    result += Html.ActionLink("Sửa ", "Edit", "DoanVien",
                                                            new
                                                            {
                                                                id = item.Id
                                                            },
                                                            new { @title = "chỉnh sửa" }
                                                            ).ToString();
                                    result += Html.ActionLink("Xóa ", "Delete", "DoanVien",
                                                            new
                                                            {
                                                                id = item.Id
                                                            },
                                                            new { @title = "Xóa" }
                                                            ).ToString();
                                    return result;
                                }

                                }

                            @{ var grid = new WebGrid(Model, rowsPerPage: 10, fieldNamePrefix: "sortSBs", pageFieldName: "pageSBs");
                                @grid.GetHtml(
                                        tableStyle: "webgrid table table-hover",
                                        headerStyle: "webgrid-header",
                                        footerStyle: "webgrid-footer",
                                        alternatingRowStyle: "webgrid-alternating-row",
                                        selectedRowStyle: "webgrid-selected-row",
                                        rowStyle: "webgrid-row-style",
                                        mode: WebGridPagerModes.All,
                                            firstText: "<<",
                                            lastText: ">>",
                                        columns: grid.Columns(
                                                        grid.Column(columnName: "UserName", header: "Tên ĐN"),
                                                        grid.Column(columnName: "FullName", header: "Họ tên"),
                                                        grid.Column(columnName: "Email", header: "Email"),
                                                        grid.Column(columnName: "Country", header: "Quê quán"),
                                                        grid.Column(columnName: "CreateDate", header: "Ngày Tạo"),
                                                        grid.Column(columnName: null, header: "Lựa Chọn",
                                                        format: (item) => new HtmlString(@GenerateLinks(item))
                                                    )
                                                    )
                                                )

                            }

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
